# todo

- provide a way for Maia to override IAuditable setting of datetime
- Maia will create its own MaiaBaseController with the delete action
- We may want to update build scripts to just pull Atlas (instead of using a nuget pkg)

- i think some of the program.cs can be moved into atlas for standardization and simpler program.cs (just try not to hide logic from dev)